import { Router } from 'express';

const router = Router();

// To be implemented
// router.route('/').post(protect, createSale).get(protect, getSales);
// router.route('/:id').get(protect, getSale);

export default router;
