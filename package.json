{
  "name": "pos-system-root",
  "version": "1.0.0",
  "scripts": {
    "emulate": "cross-env PATH=\"C:\\Program Files\\Java\\jdk-21\\bin;%PATH%\" firebase emulators:start --import=./firestore-data --export-on-exit --only functions,firestore,auth,ui,hosting",
    "build": "npm run build --prefix backend && npm run build --prefix backend/functions",
    "start": "npm run build && npm run emulate"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "cross-env": "^7.0.3",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.11"
  },
  "dependencies": {
    "@radix-ui/react-tabs": "^1.1.12",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "firebase": "^12.1.0",
    "tailwind-merge": "^3.3.1"
  }
}
